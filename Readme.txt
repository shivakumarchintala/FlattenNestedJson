# please keep the properties file in the same folder.

This is complete code that performs the following tasks:


	1. Reading only JSON files from file storage continuously stops only when there are no JSON files left.

	2. Flattens the structure if required.
	
	3. Dumps the data into the target table by making changes to the schema if needed. In this situation "spark-warehouse".